/**
 * Simplified Tenant Branding Types
 * Uses unified theme - no dynamic color changes
 */

export interface TenantBranding {
  logo?: string;
  // Removed theme colors - using unified theme
}

// Removed DEFAULT_THEMES - using unified theme from rulebook